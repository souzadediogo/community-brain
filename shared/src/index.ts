/**
 * @brain/shared - Shared types and schemas for Community Brain
 *
 * This package provides TypeScript types and Zod validation schemas
 * used across all Community Brain services (frontend, backend, gateway).
 */

// Export all types
export * from './types'

// Export all schemas
export * from './schemas'
